:-op(400,xfx,when).
:-op(750,xfy,>>).
:-op(700,xfx,>->).
:-op(600,xfy,=>).
:-op(500,xfy,++).

red(A,B) :- A >-> B.

A >-> C :- A >> B, B>->C.
A >-> A.

    
/*
A >=> B :- A => B.
A >=> B :- 
    A=..[G,L], 
    L1 is L, 
    B=..[G,L1].    

A+B >=> C :- A >> A1, B >> B1,
    number(A1), number(B1),
    C is A1+B1.
A+B >=> A1+B :- A >=> A1.
A+B >=> A+B1 :- B >=> B1.

A*B >=> C :- A >> A1, B >> B1,
    number(A1), number(B1),
    C is A1*B1.
    
A*B >=> A1*B :- A >=> A1.
A*B >=> A*B1 :- B >=> B1.

A when B >=> A :- B.    

fib(0) => 1.
fib(1) => 1.
fib(N) when (N>1) => fib(N-1)+fib(N-2).
    
fact(0) => 1.
fact(N) when (N>0) => N*fact(N-1).
*/
